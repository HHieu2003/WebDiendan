<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Bài viết: <%= post.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
</head>
<body>
  ư<%- include('partials/navbar') %>
  <div class="container mt-4">
    <h1 class="mb-4">Bài viết: <%= post.title %></h1>
    <div class="card mb-4">
      <div class="card-body">
        <p class="card-text"><%= post.content %></p>
        <p class="card-text text-muted">Bởi: <%= post.user ? post.user.name : 'Người dùng không xác định' %></p>
      </div>
    </div>
    <h2 class="mb-3">Bình luận</h2>
    <% if (isAuthenticated) { %>
      <div class="card mb-4">
        <div class="card-body">
          <form action="/forum/topic/<%= forumId %>/post/<%= post._id %>/comment" method="POST" class="needs-validation" novalidate>
            <div class="mb-3">
              <label for="content" class="form-label">Nội dung:</label>
              <textarea class="form-control" id="content" name="content" required></textarea>
              <div class="invalid-feedback">Vui lòng nhập nội dung bình luận.</div>
            </div>
            <button type="submit" class="btn btn-primary">Gửi</button>
          </form>
        </div>
      </div>
    <% } else { %>
      <p class="alert alert-warning mb-3">Vui lòng <a href="/auth/login" class="link-primary">đăng nhập</a> để bình luận.</p>
    <% } %>
    <div class="list-group">
      <% comments.forEach(comment => { %>
        <div class="list-group-item">
          <p class="mb-1"><%= comment.content %></p>
          <small class="text-muted">Bởi: <%= comment.user ? comment.user.name : 'Người dùng không xác định' %> - <%= new Date(comment.createdAt).toLocaleDateString() %></small>
        </div>
      <% }) %>
    </div>
    <div class="mt-4 text-center">
      <a href="/forum/topic/<%= forumId %>" class="btn btn-secondary">Quay lại Chủ đề</a>
    </div>
  </div>
  <script>
    (function () {
      'use strict'
      var forms = document.querySelectorAll('.needs-validation')
      Array.prototype.slice.call(forms)
        .forEach(function (form) {
          form.addEventListener('submit', function (event) {
            if (!form.checkValidity()) {
              event.preventDefault()
              event.stopPropagation()
            }
            form.classList.add('was-validated')
          }, false)
        })
    })()
  </script>
</body>
</html>